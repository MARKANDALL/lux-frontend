# Nightly Health Scan — 2026-04-19

## Summary
3 localStorage violations | 0 silent catches | 0 broken imports | 1 url drift | 0 missing mounts

## Top Priority
**HIGH — broken production path.** `ui/auth-dom.js:191` calls `apiFetch("/_api/migrate", …)`; the `/_api/` prefix has no proxy in `vite.config.js` or `vercel.json`, so guest→user history migration fails silently in both dev and prod. Should be `` apiFetch(`${API_BASE}/api/migrate`, …) ``.

## Findings

### 1. Raw localStorage outside lux-storage.js
- `features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` should route through a `lux-storage.js` helper (e.g. `getString`/`getJSON`).
- `features/harvard/modal-favs.js:19` — direct `localStorage.getItem(K_PASSAGES_FAVS)`; same pattern.
- `features/harvard/modal-favs.js:33` — direct `localStorage.setItem(K_HARVARD_FAVS, …)`; should use a `setJSON`-style helper.
- `features/harvard/modal-favs.js:36` — direct `localStorage.setItem(K_PASSAGES_FAVS, …)`.
- `features/streaming/transport/session-bootstrap.js:13` — `localStorage.getItem(K_ADMIN_TOKEN)` fallback read; consider centralising (util.js already owns this key, but this file is not a key-owner).
- `features/streaming/transport/session-bootstrap.js:21` — matching `localStorage.setItem(K_ADMIN_TOKEN, t)` write.
- `public/lux-popover.js:5` — `localStorage.getItem(KEY)` for user id; `public/` is served as-is and bypasses the storage wrapper.

### 2. Silent catches
✅ Clean

### 3. Broken imports
✅ Clean

### 4. URL drift
- `ui/auth-dom.js:191` — `apiFetch("/_api/migrate", …)` uses the repo folder name as a URL prefix. `/_api/` is not proxied in `vite.config.js` (dev) and has no rewrite in `vercel.json` (prod), so the call 404s everywhere. Canonical pattern is `` `${API_BASE}/api/migrate` `` per every working helper in `_api/`.

### 5. Missing mounts
✅ Clean

## Legacy Backlog
✅ None

## Notes
- Only one substantive commit in the last 24h: `bb3c687 fix: XSS on summary.js err.word + warnSwallow typeof guards + README link`. The XSS fix (escapeHtml on `err.word`) and the two `warnSwallow?.(…)` optional-chain guards in `features/harvard/modal-actions.js` look correct and did not introduce any of the findings above.
- `_api/identity.js` and `_api/util.js` localStorage calls are excluded per the standing exclusion rule (legacy key-owner files — intentional).
- `ui/lux-warn.js` internal catches (lines 11, 21, 72) were ignored per the standing exclusion for the logger's own error handling.
