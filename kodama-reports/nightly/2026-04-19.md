# Nightly Health Scan — 2026-04-19

## Summary
7 localStorage violations | 1 silent catch | 0 broken imports | 1 url drift | 0 missing mounts

## Top Priority
**HIGH:** `ui/auth-dom.js:191` passes `/_api/migrate` to `apiFetch()` — the `/_api/` prefix has no dev proxy rewrite (vite.config.js only proxies `/api`) and no prod rewrite (vercel.json), so the migration call 404s in every environment.

## Findings

### 1. Raw localStorage outside lux-storage.js
- `features/harvard/modal-favs.js:9` — direct `localStorage.getItem(K_HARVARD_FAVS)`; should route through `getJSON` in lux-storage.js.
- `features/harvard/modal-favs.js:19` — direct `localStorage.getItem(K_PASSAGES_FAVS)`; same as above.
- `features/harvard/modal-favs.js:33` — direct `localStorage.setItem(K_HARVARD_FAVS, ...)`; should use `setJSON`.
- `features/harvard/modal-favs.js:36` — direct `localStorage.setItem(K_PASSAGES_FAVS, ...)`; should use `setJSON`.
- `features/streaming/transport/session-bootstrap.js:13` — direct `sessionStorage.getItem(K_ADMIN_TOKEN) || localStorage.getItem(K_ADMIN_TOKEN)`.
- `features/streaming/transport/session-bootstrap.js:21` — direct `localStorage.setItem(K_ADMIN_TOKEN, t)` alongside session write.
- `public/lux-popover.js:5` — direct `localStorage.getItem("LUX_USER_ID")` with a hard-coded key string (comment notes it mirrors `K_IDENTITY_UID`).

### 2. Silent catches
- `ui/components/trouble-chips.js:168` — `catch (_) { /* safe */ }` wraps `panel.scrollIntoView(...)` with no logging, no warnSwallow, no fallback action.

### 3. Broken imports
✅ Clean

### 4. URL drift
- `ui/auth-dom.js:191` — `apiFetch("/_api/migrate", ...)` uses the `/_api/` folder prefix as a URL path. No server handler exists at that route; must be `${API_BASE}/api/migrate` through the canonical pattern.

### 5. Missing mounts
✅ Clean

## Legacy Backlog
✅ None (all flagged files were modified within the last 30 days).

## Notes
- Activity gate: 2 substantive commits in the last 24h touching `features/harvard/modal-actions.js` and `features/results/summary.js` (plus README). Neither touched file introduced any of the findings above; the scan surfaced pre-existing issues across the repo per the standing rubric.
- `_api/identity.js` and `_api/util.js` were skipped for the raw-localStorage check per the standing exclusion — these are legacy key-owner files and their direct `localStorage` calls are intentional.
- `app-core/lux-storage.test.js` was also skipped for the raw-localStorage check — the test file intentionally monkey-patches `localStorage.getItem/setItem` to exercise quota-error paths in `lux-storage.js` itself.
- `ui/lux-warn.js` catch blocks were excluded from the silent-catch check per standing rule (the logger's own error handling is by design).
- The `helpers/index.js:18` `export { ... } from "./media.js"` resolves to a non-existent path, but the statement is inside a `/* ... */` block comment, so it is not an active import — not flagged.
