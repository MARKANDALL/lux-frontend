# Nightly Health Scan — 2026-04-20

## Summary
[7] localStorage violations | [3] silent catches | [0] broken imports | [0] url drift | [0] missing mounts

## Top Priority
`features/harvard/modal-favs.js` already imports `K_HARVARD_FAVS` / `K_PASSAGES_FAVS` from `app-core/lux-storage.js` yet still hits raw `localStorage` on 4 lines — the lowest-effort, highest-signal cleanup: swap to the `getString` / `setString` helpers for a clean routing through the storage module.

## Findings

### 1. Raw localStorage outside lux-storage.js
- `features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` bypasses lux-storage helpers (file already imports the key constant).
- `features/harvard/modal-favs.js:19` — `localStorage.getItem(K_PASSAGES_FAVS)` same pattern.
- `features/harvard/modal-favs.js:33` — `localStorage.setItem(K_HARVARD_FAVS, …)` same pattern.
- `features/harvard/modal-favs.js:36` — `localStorage.setItem(K_PASSAGES_FAVS, …)` same pattern.
- `features/streaming/transport/session-bootstrap.js:13` — `sessionStorage.getItem ∥ localStorage.getItem(K_ADMIN_TOKEN)` duplicates the admin-token read already provided by `_api/util.js::getAdminToken()`.
- `features/streaming/transport/session-bootstrap.js:21` — `localStorage.setItem(K_ADMIN_TOKEN, t)` duplicates write path; should reuse the `_api/util.js` owner.
- `public/lux-popover.js:5` — `localStorage.getItem("LUX_USER_ID")` hard-codes the key string (file is a plain IIFE served from `public/` so it cannot ES-import lux-storage; worth a dedicated getter exposed on `window` or a build step).

### 2. Silent catches
- `ui/components/trouble-chips.js:168` — `catch (_) { /* safe */ }` wraps `panel.scrollIntoView(...)`; no logging or rethrow.
- `features/recorder/audio-mode-core.js:64` — `catch { /* ignore storage failures (private mode etc.) */ }` wraps `setString(K_AUDIO_MODE, safe)`; discards storage errors silently.
- `features/streaming/transport/realtime-webrtc/mic-meter.js:53` — `catch { /* never fail connect because of a meter */ }` wraps the full meter-attach block; no diagnostic channel if WebAudio setup breaks.

### 3. Broken imports
✅ Clean

### 4. URL drift
✅ Clean

### 5. Missing mounts
✅ Clean

## Legacy Backlog
✅ None (all flagged files modified within the last 30 days).

## Notes
- Last 24h of substantive commits touched: `helpers/core.js`, `helpers/index.js`, `helpers/md-to-html.js`, `ui/auth-dom.js`, `ui/ui-ai-ai-dom.js`, `features/progress/attempt-detail-modal.js`, `features/progress/attempt-detail/ai-coach-section.js`, `features/progress/progress-utils.js`, and several tests. None of those files surfaced findings — today's flags are pre-existing drift, not fresh regressions.
- Excluded from Check 1 per policy: `_api/util.js` and `_api/identity.js` (documented legacy key-owners whose direct localStorage calls are intentional). `app-core/lux-storage.test.js` is the storage module's own test file and is also excluded.
- Excluded from Check 2 per policy: `ui/lux-warn.js` (logger's own error handling is by design).
- All three silent catches carry explanatory comments — they are defensive by intent. Flagged because they match the `/* ignore */`-equivalent pattern and discard context; worth converting to `warnSwallow` or a debug-level log to keep diagnostics routed through the canonical channel.
