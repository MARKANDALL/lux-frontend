# Nightly Health Scan — 2026-04-24

## Summary
7 localStorage violations | 9 silent catches | 0 broken imports | 0 url drift | 0 missing mounts

## Top Priority
`features/streaming/transport/session-bootstrap.js:13` bypasses the canonical storage layer for the admin token (raw `sessionStorage`/`localStorage` on `K_ADMIN_TOKEN`); worth routing through `lux-storage.js` since it also prompts the user for and persists a privileged token.

## Findings

### 1. Raw localStorage outside lux-storage.js
- `features/streaming/transport/session-bootstrap.js:13` — raw `localStorage.getItem(K_ADMIN_TOKEN)` (and `sessionStorage.getItem` on the same line) bypasses lux-storage wrappers.
- `features/streaming/transport/session-bootstrap.js:21` — raw `localStorage.setItem(K_ADMIN_TOKEN, t)` for the admin token.
- `features/harvard/modal-favs.js:9` — raw `localStorage.getItem(K_HARVARD_FAVS)` in `loadFavs()`.
- `features/harvard/modal-favs.js:19` — raw `localStorage.getItem(K_PASSAGES_FAVS)` in `loadFavs()`.
- `features/harvard/modal-favs.js:33` — raw `localStorage.setItem(K_HARVARD_FAVS, …)` in `saveFavs()`.
- `features/harvard/modal-favs.js:36` — raw `localStorage.setItem(K_PASSAGES_FAVS, …)` in `saveFavs()`.
- `public/lux-popover.js:5` — raw `localStorage.getItem(KEY)` to read the user id in the standalone popover script.

### 2. Silent catches
- `features/streaming/transport/realtime-webrtc/mic-meter.js:53` — empty catch body swallows all mic-meter setup errors during WebRTC connect.
- `features/streaming/transport/realtime-webrtc/message-handler.js:37` — `catch { return; }` on `JSON.parse(e.data)` drops malformed realtime events with no trace.
- `features/interactions/boot.js:15` — empty catch in the dynamic-import loader hides per-path failures before the final `console.warn`.

### 3. Broken imports
✅ Clean

### 4. URL drift
✅ Clean

### 5. Missing mounts
✅ Clean

## Additional Findings
- `features/recorder/audio-mode-core.js:64`
- `ui/warp-nav.js:19`
- `ui/components/trouble-chips.js:168`
- `app-core/lux-storage.js:23`
- `features/life/storage.js:10`
- `features/my-words/service.js:12`

## Legacy Backlog
✅ None (all flagged files modified within the last 30 days).

## Notes
- 24-hour commit window: `14b6918`, `67475c2`, `53b6561`, `021e274` — touched `features/features/selfpb/dom.js`, `features/features/selfpb/styles/expanded.js`, `features/features/tts/player-ui.js`, `features/voice-mirror/voice-mirror.js`. None of these files produced findings; the report above reflects pre-existing backlog surfaced by a full-repo scan, not new regressions from the last day.
- Per rules, `_api/identity.js` and `_api/util.js` were excluded from the raw-localStorage check as legacy key-owner files.
- `public/vendor/wavesurfer-7.8.11.min.js` catch hits were ignored as third-party minified vendor code.
- `mountKnobsDrawer` (features/convo/knobs-drawer.js:212) is not imported by any external module, but it is invoked internally by `getKnobsDrawerInstance()` on line 273 — not flagged.
