# Nightly Health Scan — 2026-04-17

## Summary
14 localStorage violations | 80 silent catches | 0 broken imports | 1 url drift | 0 missing mounts

## Findings

### 1. Raw localStorage outside lux-storage.js
- `_api/util.js:54` — `localStorage.getItem(K_ADMIN_TOKEN)` in `getAdminToken()` — should use `getString(K_ADMIN_TOKEN)`.
- `_api/util.js:62` — `localStorage.setItem(K_ADMIN_TOKEN, t)` in `getAdminToken()` prompt path — should use `setString`.
- `_api/identity.js:59` — `localStorage.getItem(KEY)` in `ensureUID()`.
- `_api/identity.js:60` — `localStorage.getItem(ALIAS_KEY)` in `ensureUID()`.
- `_api/identity.js:61` — `localStorage.getItem(LEGACY_KEY)` in `ensureUID()`.
- `_api/identity.js:87` — `localStorage.setItem(KEY, finalUID)` in `ensureUID()`.
- `_api/identity.js:88` — `localStorage.setItem(ALIAS_KEY, finalUID)` in `ensureUID()`.
- `_api/identity.js:90` — `localStorage.setItem(LEGACY_KEY, finalUID)` in `ensureUID()`.
- `_api/identity.js:127` — `localStorage.setItem(KEY, u)` in `setUID()`.
- `_api/identity.js:128` — `localStorage.setItem(ALIAS_KEY, u)` in `setUID()`.
- `_api/identity.js:129` — `localStorage.setItem(LEGACY_KEY, u)` in `setUID()`.
- `features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` in `loadFavs()` — should use `getJSON`.
- `features/harvard/modal-favs.js:19` — `localStorage.getItem(K_PASSAGES_FAVS)` in `loadFavs()` — should use `getJSON`.
- `features/harvard/modal-favs.js:33` — `localStorage.setItem(K_HARVARD_FAVS, ...)` in `saveFavs()` — should use `setJSON`.
- `features/harvard/modal-favs.js:36` — `localStorage.setItem(K_PASSAGES_FAVS, ...)` in `saveFavs()` — should use `setJSON`.
- `features/streaming/transport/session-bootstrap.js:13` — `localStorage.getItem(K_ADMIN_TOKEN)` in `getClientToken()`.
- `features/streaming/transport/session-bootstrap.js:21` — `localStorage.setItem(K_ADMIN_TOKEN, t)` in `getClientToken()` prompt path.
- `public/lux-popover.js:5` — `localStorage.getItem(KEY)` with bare string literal `"LUX_USER_ID"` — standalone popover script, should at minimum use `K_IDENTITY_UID`.

Count: 18 lines across 5 files (14 flagged call sites — 4 in identity.js are paired get/set around the same three keys).

### 2. Silent catches without warnSwallow()
Scan flagged 80 catch blocks that swallow errors without routing through `warnSwallow()`. Breakdown:

**Returns fallback value, no reporting (20):**
- `app-core/lux-storage.js:23` — `_get()` returns `null` on getItem failure.
- `app-core/lux-storage.js:63` — `getJSON()` returns fallback on `JSON.parse` failure.
- `features/convo/convo-api.js:11` — `convoTurnWithUi()` returns fallback message.
- `features/convo/picker-deck/cefr-hint-badge.js:67` — returns `"B1"` fallback.
- `features/harvard/index.js:22` — `safeParseJson()` returns fallback.
- `features/harvard/index.js:44` — `readRandomBag()` returns `[]` fallback.
- `features/life/app.js:11` — `randSeed32()` fallback.
- `features/life/storage.js:10` — `loadLifeRun()` returns `null` on parse error.
- `features/my-words/service.js:12` — `getAuthedUID()` returns `null` on auth failure.
- `features/my-words/store.js:10` — `safeParse()` fallback.
- `features/next-activity/next-practice.js:97` — `labelForPassageKey()` fallback.
- `features/progress/wordcloud/side-drawers.js:11` — `safeParse()` fallback.
- `features/recorder/audio-mode-core.js:51` — `getAudioMode()` fallback.
- `features/recorder/audio-mode.js:52` — `supportedConstraints()` fallback.
- `features/streaming/transport/realtime-webrtc/message-handler.js:37` — silent early return.
- `ui/ui-ai-ai-logic/attempt-policy.js:17` — `getLuxSessionInt()` returns 0.
- `ui/warp-nav.js:19` — `catch { return; }` on URL parse failure.

**Logs via `console.warn`/`console.error` instead of `warnSwallow` (54):**
- `_api/attempts.js:103`
- `app-core/audio-sink.js:41`
- `features/convo/convo-flow.js:144`
- `features/convo/convo-handlers.js:227`, `:246`
- `features/convo/convo-persistence.js:29`
- `features/convo/convo-report-ui.js:247`
- `features/convo/convo-turn.js:75`, `:111`
- `features/convo/picker-deck.js:34`
- `features/convo/progress.js:85`, `:154`
- `features/dashboard/index.js:109`, `:178`, `:279`
- `features/features/08-selfpb-peekaboo.js:165`, `:214`
- `features/features/selfpb/controls.js:59`
- `features/features/selfpb/core.js:125`, `:155`
- `features/features/tts/boot-tts.js:92`
- `features/features/tts/player-ui.js:402`
- `features/harvard/index.js:68`, `:217`
- `features/harvard/modal-controller.js:139`
- `features/harvard/modal-data.js:14`
- `features/interactions/boot.js:31`
- `features/my-words/index.js:89`, `:164`
- `features/progress/wordcloud/render.js:264`
- `features/progress/wordcloud/render/runner.js:128`
- `features/progress/wordcloud/timeline.js:82`
- `features/recorder/index.js:241`
- `features/results/header.js:95`, `:103`, `:294`
- `features/results/index.js:25`, `:41`
- `features/results/summary-shell.js:138`
- `features/streaming/app.js:174`, `:184`, `:288`
- `features/streaming/state/store.js:22`
- `features/streaming/transport/transport-controller.js:120`, `:152`, `:161`, `:174`, `:201`
- `features/voice-mirror/voice-mirror.js:13`, `:33`, `:349`
- `features/voice-mirror/voice-onboarding.js:375`, `:434`
- `public/lux-popover.js:91`
- `ui/auth-dom.js:196` — `catch (e) { console.error("[Auth] Migration failed:", e); }` — paired with the `/_api/` drift below.
- `ui/ui-ai-ai-logic/deep-mode.js:150`
- `ui/ui-ai-ai-logic/quick-mode.js:138`

**Empty body / comments-only (3):**
- `features/interactions/boot.js:15` — `catch (e) { // swallow and try next }`.
- `features/streaming/transport/realtime-webrtc/mic-meter.js:53`.
- `ui/components/trouble-chips.js:168`.

Not flagged: `ui/lux-warn.js` internal catches (canonical helper, whitelisted).

### 3. Broken / commented-out imports
✅ Clean. All 500+ relative imports resolve to on-disk files (or to `index.js` under a directory). No `// import` / `/* import` leftovers outside of documentation examples in `app-core/lux-bus.js` and `app-core/lux-listeners.js` file headers.

### 4. URL drift
- `ui/auth-dom.js:191` — `apiFetch("/_api/migrate", { ... })`. The `_api/` prefix is a folder name only; no server route exists. Vite dev proxy only forwards `/api/`, and `vercel.json` has no `/_api/` rewrite. This call 404s in every environment. Canonical form: `apiFetch(\`${API_BASE}/api/migrate\`, ...)` (plus importing `API_BASE` from `../_api/util.js`, which this file already does on line 9).

All other helper files use the canonical `${API_BASE}/api/...` pattern. No bare `/api/` paths in runtime call sites (matches in `_api/util.test.js` are mocked tests; `_api/util.js:9` and `:75` are comments; `_api/attempts.test.js:31` is a test assertion).

### 5. Missing mounts
✅ Clean. All 13 exported `mount*` functions have at least one consumer:
- `mountVoiceMirrorButton` → `features/results/summary.js:13`, `features/features/tts/player-ui.js:24`.
- `mountSyllablesForTable` → dynamic import in `features/interactions/score-collapse.js:18`, `features/results/render-core.js:39`.
- `mountAICoachAlwaysOn` → `features/recorder/index.js:21`, `features/dashboard/index.js:16`, `features/convo/convo-ai-coach-shell.js:3`.
- `mountMyWordsPanel`, `mountMyWordsCornerLauncher` → `features/my-words/index.js`, `boot.js`, `library-modal.js`.
- `mountLifeApp`, `mountStreamingApp`, `mountStreamingSetupApp`, `mountAudioModeSwitch`, `mountTTSPlayer`, `mountCefrHintBadge`, `mountKnobsDrawer`, `mountSelfPlaybackLite` — all wired to their respective entry points.

## Notes
- Last 24h commits: `a9acfab` (ARCHITECTURE-2.md draft), `bb816d1` (alt-meaning URL API_BASE fix), `e05ea30` (routine prompt fixes). Only one source file (`_api/alt-meaning.js`) changed — inspected and canonical ✅.
- The single URL drift (`ui/auth-dom.js:191`) predates the 24h window; paired with a silent `catch` that `console.error`s on the 404 rather than routing through `warnSwallow`, which is why the breakage is invisible in production. Same file is the migration path triggered after login, so failures here silently lose guest history on authentication.
- `app-core/lux-storage.js:23` and `:63` are silent catches inside the canonical storage helper itself — a gap worth closing before the remaining migrations land, so every silent-catch regression downstream has one consistent reporting mechanism.
- Raw-localStorage cluster in `_api/identity.js` and `_api/util.js` is the Simoishi "bare-localstorage-stage-1" migration target per recent commits (`64e3e84`, `4128029`, `b98f2b9`); scheduled work, not a regression.
- Voice-mirror and streaming subsystems account for the largest concentrations of silent catches (21/80). Good candidates for the next `silent-catches` migration batch.
