# Nightly Health Scan — 2026-04-15

## Summary
[18] localStorage violations | [3] silent catches | [2] broken imports | [2] api path drift | [0] missing mounts

---

## Findings

### 1. Raw localStorage outside lux-storage.js

> **Note:** The check spec references `helpers/lux-storage.js` but the actual canonical module is `app-core/lux-storage.js`. Violations are any `localStorage.*` calls outside that file. None of these were introduced in the last 24 h; all are pre-existing.

`_api/identity.js:59` — `localStorage.getItem(KEY)` — raw identity UID read, bypasses `app-core/lux-storage.js` helpers.  
`_api/identity.js:60` — `localStorage.getItem(ALIAS_KEY)` — raw alias read.  
`_api/identity.js:61` — `localStorage.getItem(LEGACY_KEY)` — raw legacy-key read.  
`_api/identity.js:87` — `localStorage.setItem(KEY, finalUID)` — raw UID write.  
`_api/identity.js:88` — `localStorage.setItem(ALIAS_KEY, finalUID)` — raw alias write.  
`_api/identity.js:90` — `localStorage.setItem(LEGACY_KEY, finalUID)` — raw legacy write.  
`_api/identity.js:127` — `localStorage.setItem(KEY, u)` — raw UID overwrite.  
`_api/identity.js:128` — `localStorage.setItem(ALIAS_KEY, u)` — raw alias overwrite.  
`_api/identity.js:129` — `localStorage.setItem(LEGACY_KEY, u)` — raw legacy overwrite.  
`_api/util.js:54` — `localStorage.getItem(K_ADMIN_TOKEN)` — raw admin-token read (key imported from lux-storage but bypasses helper wrapper).  
`_api/util.js:62` — `localStorage.setItem(K_ADMIN_TOKEN, t)` — raw admin-token write.  
`features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` — Harvard favourites raw read.  
`features/harvard/modal-favs.js:19` — `localStorage.getItem(K_PASSAGES_FAVS)` — Passages favourites raw read.  
`features/harvard/modal-favs.js:33` — `localStorage.setItem(K_HARVARD_FAVS, ...)` — Harvard favourites raw write.  
`features/harvard/modal-favs.js:36` — `localStorage.setItem(K_PASSAGES_FAVS, ...)` — Passages favourites raw write.  
`features/streaming/transport/session-bootstrap.js:13` — `localStorage.getItem(K_ADMIN_TOKEN)` — admin-token raw read during session bootstrap.  
`features/streaming/transport/session-bootstrap.js:21` — `localStorage.setItem(K_ADMIN_TOKEN, t)` — admin-token raw write.  
`public/lux-popover.js:5` — `localStorage.getItem(KEY)` — popover script reads UID directly; lux-storage not available in this standalone context.

---

### 2. Silent catches without warnSwallow()

> Spec definition: any `catch` block whose body does not invoke `warnSwallow` or rethrow the error.  
> **Note:** The spec references `helpers/warn-swallow.js` which does not exist on disk. The canonical implementation is `ui/lux-warn.js`, exposing `globalThis.warnSwallow` / `window.warnSwallow`.

`ui/components/trouble-chips.js:168` — `catch (_) { /* safe */ }` — completely empty body; swallows `scrollIntoView` error with no reporting.  
`features/life/storage.js:10` — `catch (_) { return null; }` — error on `getJSON` silently discarded; only returns null fallback, no warnSwallow.  
`features/life/app.js:11` — `catch (_) { return (Date.now() >>> 0) ^ ...; }` — `crypto.getRandomValues` failure silently falls back to Date-based seed; no warnSwallow.

---

### 3. Broken imports

**Commented-out import statements (// import or /* import):**

`app-core/lux-bus.js:7` — `//   import { luxBus } from '../app-core/lux-bus.js';` — commented-out import in module-header usage example.  
`app-core/lux-listeners.js:7` — `//   import { guardedListener } from '../app-core/lux-listeners.js';` — same pattern.

> Context: Both lines appear inside a multi-line `// Usage:` comment block at the top of the file. They are documentation, not dead code. Flagged per spec literal.

**Broken import paths (reference to non-existent file):**

✅ None found. All relative imports across the in-scope file tree resolve to existing paths. Commit `5012444` ("fix: resolve April audit RED bugs — restore missing imports") successfully restored the previously-missing imports in `features/harvard/modal-actions.js` and `features/results/summary.js`.

---

### 4. API path drift — `/api/` that should be `/_api/`

`ui/auth-dom.js:191` — `apiFetch("/api/migrate", { method: "POST", ... })` — live auth migration call using pre-March-2026 path; will 404 on Vercel production.  
`_api/alt-meaning.js:6` — `` const ALT_MEANING_URL = `/api/router?route=alt-meaning`; `` — browser-side alt-meaning helper still targets `/api/router`; every alt-meaning lookup fails on production.

> Also noted (not executable code): `docs/ARCHITECTURE.md:249` contains a code example using `/api/some-route`; `_api/util.js:75` has a JSDoc comment with `/api/some-route`. Documentation drift only.

---

### 5. Missing mounts

✅ Clean. All `mount*` functions with external consumers are imported correctly:

| Function | Defined in | Imported by |
|---|---|---|
| `mountVoiceMirrorButton` | `features/voice-mirror/voice-mirror.js` | `features/results/summary.js`, `features/features/tts/player-ui.js` |
| `mountKnobsDrawer` | `features/convo/knobs-drawer.js` | Called internally by `getKnobsDrawerInstance` (same file), which is imported by `convo-knobs-ui.js`, `convo-modes.js`, `convo-picker-system.js` |
| `mountCefrHintBadge` | `features/convo/picker-deck/cefr-hint-badge.js` | `features/convo/picker-deck/deck-card.js` |
| `mountSelfPlaybackLite` | `features/features/selfpb/ui.js` | `features/features/08-selfpb-peekaboo.js` (dynamic import) |
| `mountTTSPlayer` | `features/features/tts/player-ui.js` | `features/features/tts/boot-tts.js` (dynamic import) |
| `mountLifeApp` | `features/life/app.js` | `src/life.js` |
| `mountAudioModeSwitch` | `features/recorder/audio-mode-switch.js` | `features/convo/convo-bootstrap.js`, `features/recorder/index.js`, `src/main.js` |
| `mountStreamingApp` | `features/streaming/app.js` | `src/stream.js` |
| `mountStreamingSetupApp` | `features/streaming/setup/app.js` | `src/stream-setup.js` |
| `mountMyWordsCornerLauncher` | `features/my-words/launcher.js` | `features/my-words/boot.js`, `features/my-words/index.js` |
| `mountMyWordsPanel` | `features/my-words/panel.js` | `features/my-words/index.js`, `features/my-words/library-modal.js` |
| `mountAICoachAlwaysOn` | `ui/ui-ai-ai-logic.js` | `features/convo/convo-ai-coach-shell.js`, `features/dashboard/index.js`, `features/recorder/index.js` |
| `mountSyllablesForTable` | `features/results/syllables.js` | `features/interactions/score-collapse.js`, `features/results/render-core.js` (dynamic) |

---

## Notes

- **helpers/lux-storage.js does not exist.** The spec's check references this path, but the actual canonical module is `app-core/lux-storage.js`. The gap between the spec and the codebase may cause false-negative expectations in automated tooling.
- **helpers/warn-swallow.js does not exist.** The canonical warnSwallow implementation lives in `ui/lux-warn.js` (exported as `window.warnSwallow` / `globalThis.warnSwallow`).
- **ARCHITECTURE.md still references `/api/` (not `/_api/`).** The code example at `docs/ARCHITECTURE.md:249` shows `apiFetch('/api/some-route', ...)` — a developer following the docs will produce a path-drift bug.
- **Commit `5012444` is clean.** The "restore missing imports" fix correctly added imports to `modal-actions.js` (line 7) and `summary.js` (lines 8–13). All resolved paths verified on disk.
- **`modal-actions.js:7` has 3 leading spaces on its import statement.** Syntactically valid but inconsistent with the rest of the file; may confuse linters that check module-level scope.
- **Scope of this scan:** 386 `.js/.jsx/.html/.css` files, excluding `node_modules/`, `dist/`, `_ARCHIVE/`, `_agents-archive/`, `_parts/`, and any other `_*` directory. `_api/` was included per spec.
