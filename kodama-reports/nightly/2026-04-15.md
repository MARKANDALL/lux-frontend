# Nightly Health Scan — 2026-04-15

## Summary
[7] localStorage violations | [3] silent catches | [0] broken imports | [1] api path drift | [0] missing mounts

---

## Findings

### 1. Raw localStorage outside lux-storage.js

`public/lux-popover.js:5` — `localStorage.getItem(KEY)` called directly; file is a standalone IIFE but must route through `app-core/lux-storage.js` helpers like everything else.

`features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` called raw inside `loadFavs()`; key constant is imported from lux-storage but the read bypasses the typed `getJSON()` helper.

`features/harvard/modal-favs.js:19` — `localStorage.getItem(K_PASSAGES_FAVS)` — same pattern, second key.

`features/harvard/modal-favs.js:33` — `localStorage.setItem(K_HARVARD_FAVS, ...)` called raw inside `saveFavs()`; should use `setJSON()`.

`features/harvard/modal-favs.js:36` — `localStorage.setItem(K_PASSAGES_FAVS, ...)` — second raw write in same function.

`features/streaming/transport/session-bootstrap.js:13` — `localStorage.getItem(K_ADMIN_TOKEN)` chained inside `getClientToken()`; admin credential read bypasses the wrapper.

`features/streaming/transport/session-bootstrap.js:21` — `localStorage.setItem(K_ADMIN_TOKEN, t)` — raw admin-token write; stores credentials outside the sanctioned pattern with no error handling.

---

### 2. Silent catches

`features/harvard/modal-favs.js:14` — `catch { favs = new Set(); }` — bare catch block resets state silently; no `warnSwallow()` call, no rethrow. A failed JSON.parse would be invisible.

`features/harvard/modal-favs.js:24` — `catch { favKeys = new Set(); }` — identical pattern for the passages-favs key. Both silent catches in `loadFavs()` are adjacent but need instrumentation.

`ui/components/trouble-chips.js:168` — `catch (_) { /* safe */ }` — swallows a `scrollIntoView` error with only a comment; should call `warnSwallow("ui/components/trouble-chips.js", _, "low")`.

---

### 3. Broken imports

✅ Clean

---

### 4. API path drift

`ui/auth-dom.js:191` — `apiFetch("/api/migrate", { method: "POST", ... })` — uses the pre-March-2026 `/api/` prefix. Since the Vercel Hobby 12-function cap workaround renamed all routes to `/_api/`, this will 404 in production unless a `_api/migrate.js` function exists. No such file was found in the `_api/` directory at scan time.

---

### 5. Missing mounts

✅ Clean — all exported `mount*` functions are imported by at least one consumer.

---

## Notes

- `features/harvard/modal-favs.js` is a dual-issue concentration: 4 raw localStorage calls **and** 2 silent catches in the same 43-line file. Likely written before the lux-storage.js wrapper existed and not yet migrated.
- `features/streaming/transport/session-bootstrap.js` stores an admin credential with a raw `localStorage.setItem` and no try/catch at all on the write path (line 21). Unlike modal-favs, the key constant is imported correctly but the typed helper is not used.
- No `_api/migrate.js` exists on disk. Either the migration endpoint was never ported from `/api/` to `/_api/`, or `auth-dom.js:191` is dead code.
- ARCHITECTURE.md still references `/api/` in at least one place (not flagged here as it is a .md file, outside scan scope — worth a follow-up manual check).
- Recent commit `5012444 fix: resolve April audit RED bugs — restore missing imports` landed in the 24-hour window. The broken-imports check came back clean, consistent with that fix having landed.
