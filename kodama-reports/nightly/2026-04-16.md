# Nightly Health Scan — 2026-04-16

## Summary
18 localStorage violations | 10 silent catches | 0 broken imports | 2 url drift | 0 missing mounts

## Findings

### 1. Raw localStorage outside lux-storage.js
- `_api/util.js:54` — `localStorage.getItem(K_ADMIN_TOKEN)` inside `getAdminToken()` bypasses storage helper.
- `_api/util.js:62` — `localStorage.setItem(K_ADMIN_TOKEN, t)` bypasses storage helper.
- `_api/identity.js:59` — `localStorage.getItem(KEY)` for UID reads raw.
- `_api/identity.js:60` — `localStorage.getItem(ALIAS_KEY)` for alias reads raw.
- `_api/identity.js:61` — `localStorage.getItem(LEGACY_KEY)` for legacy UID reads raw.
- `_api/identity.js:87` — `localStorage.setItem(KEY, finalUID)` writes UID raw.
- `_api/identity.js:88` — `localStorage.setItem(ALIAS_KEY, finalUID)` writes alias raw.
- `_api/identity.js:90` — `localStorage.setItem(LEGACY_KEY, finalUID)` writes legacy raw.
- `_api/identity.js:127` — `localStorage.setItem(KEY, u)` in `setUID()` writes raw.
- `_api/identity.js:128` — `localStorage.setItem(ALIAS_KEY, u)` in `setUID()` writes raw.
- `_api/identity.js:129` — `localStorage.setItem(LEGACY_KEY, u)` in `setUID()` writes raw.
- `public/lux-popover.js:5` — `localStorage.getItem(KEY)` reads UID raw in popover bootstrap.
- `features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` reads favs raw.
- `features/harvard/modal-favs.js:19` — `localStorage.getItem(K_PASSAGES_FAVS)` reads passage favs raw.
- `features/harvard/modal-favs.js:33` — `localStorage.setItem(K_HARVARD_FAVS, …)` writes favs raw.
- `features/harvard/modal-favs.js:36` — `localStorage.setItem(K_PASSAGES_FAVS, …)` writes passage favs raw.
- `features/streaming/transport/session-bootstrap.js:13` — `localStorage.getItem(K_ADMIN_TOKEN)` reads admin token raw.
- `features/streaming/transport/session-bootstrap.js:21` — `localStorage.setItem(K_ADMIN_TOKEN, t)` writes admin token raw.

### 2. Silent catches
Catches with no `warnSwallow()` call and no rethrow. Listing the ones that take no user-visible action (truly silent); see Notes for the larger set that only `console.error/warn`.

- `features/interactions/boot.js:15` — `catch (e) { // swallow and try next }`; comment-only body during dynamic-import fallback.
- `ui/components/trouble-chips.js:168` — `catch (_) { /* safe */ }`; comment-only body.
- `ui/warp-nav.js:19` — `catch { return; }`; silently aborts URL parsing on failure.
- `features/my-words/store.js:10` — `catch { return fallback; }`; swallows JSON-parse errors for stored data.
- `features/my-words/service.js:12` — `catch { return null; }`; swallows fetch/parse error.
- `features/next-activity/next-practice.js:97` — `catch { return key; }`; swallows lookup failure.
- `features/life/storage.js:10` — `catch (_) { return null; }`; swallows read error.
- `features/life/app.js:11` — `catch (_) { return (Date.now() >>> 0) ^ …; }`; swallows UUID error.
- `public/lux-popover.js:91` — `catch { return el.src === src; }`; swallows URL comparison error.
- `ui/ui-ai-ai-logic/attempt-policy.js:17` — `catch { return 0; }`; swallows parse error.

### 3. Broken imports
✅ Clean

(Two matches for `^//\s*import` were JSDoc-style usage examples inside `app-core/lux-listeners.js:7` and `app-core/lux-bus.js:7`, not commented-out imports.)

### 4. URL drift
- `_api/alt-meaning.js:6` — `` const ALT_MEANING_URL = `/_api/router?route=alt-meaning`; `` passed to `apiFetch()` at line 9. No server handler exists for `/_api/` in dev (vite proxies `/api` only) or prod (`vercel.json` has no `/_api/` rewrite). **Regression introduced 2026-04-15 in commit `699f55b`** (message: "fix: alt-meaning API path drift"); the prior value was `/api/router?route=alt-meaning`. Canonical form should be `` `${API_BASE}/api/router?route=alt-meaning` ``.
- `ui/auth-dom.js:191` — `apiFetch("/_api/migrate", { … })` in `migrateHistory()`. Same regression from commit `699f55b`; prior value was `/api/migrate`. Canonical form should be `` `${API_BASE}/api/migrate` ``.

### 5. Missing mounts
✅ Clean

All 14 exported `mount*` functions resolve to at least one importer. `mountVoiceMirrorButton` reaches `features/results/summary.js:13` and `features/features/tts/player-ui.js:24`. `features/harvard/modal-actions.js` does not expose or consume any `mount*` — none expected. Convo entry (`convo-bootstrap.js`) imports `mountAudioModeSwitch`; no orphaned mounts.

## Notes
- **Highest severity: the two URL-drift findings are both 24h regressions from the same commit** (`699f55b`, titled "fix: alt-meaning API path drift"). The commit inverted the intended fix — it changed `/api/...` to `/_api/...`, which matches the repo folder name but has no server route. Both endpoints will 404 in production and likely also in dev (vite proxy is `/api`, not `/_api`). Worth checking whether commit `e05ea30` ("invert URL drift heuristic") in the routine prompt actually calibrates the scanner correctly; if the prompt now teaches Claude to accept `/_api/`, future scans will stop catching this.
- Silent-catch count above is the strict subset (10) that take no user-visible action. A further ~18 catches call only `console.error`/`console.warn` without `warnSwallow()`; per the rule definition those are also silent. Representative examples: `_api/attempts.js:103`, `ui/auth-dom.js:196` (same file as the URL-drift regression), `features/streaming/transport/transport-controller.js:120/152/161/174/201`, `features/streaming/app.js:174/184/288`. If desired, full list can be added next run.
- Three catches inside `ui/lux-warn.js` (lines 11, 21, 72) do not use `warnSwallow` — intentionally, since that file IS the canonical helper. Not flagged.
- `features/recorder/audio-inspector.js:4` shows `localStorage.setItem("luxAudioInspector","1")` inside a `//` comment (usage hint). Not flagged.
- `_api/util.test.js` contains `apiFetch("/api/…")` calls against a mocked fetch. Not flagged — tests, not production calls.
