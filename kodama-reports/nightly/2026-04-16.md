# Nightly Health Scan — 2026-04-16

## Summary
18 localStorage violations | 20 silent catches | 27 broken imports | 14 api path drift | 0 missing mounts

## Findings

### 1. Raw localStorage outside lux-storage.js
- `_api/util.js:54` — `localStorage.getItem(K_ADMIN_TOKEN)` in `getAdminToken` instead of `getString(K_ADMIN_TOKEN)`.
- `_api/util.js:62` — `localStorage.setItem(K_ADMIN_TOKEN, t)` should route through `setString`.
- `_api/identity.js:59` — `localStorage.getItem(KEY)` should use `getString(K_IDENTITY_UID)`.
- `_api/identity.js:60` — `localStorage.getItem(ALIAS_KEY)` should use `getString(K_IDENTITY_UID_ALIAS)`.
- `_api/identity.js:61` — `localStorage.getItem(LEGACY_KEY)` should use `getString(K_IDENTITY_UID_LEGACY)`.
- `_api/identity.js:87` — `localStorage.setItem(KEY, finalUID)` should use `setString`.
- `_api/identity.js:88` — `localStorage.setItem(ALIAS_KEY, finalUID)` should use `setString`.
- `_api/identity.js:90` — `localStorage.setItem(LEGACY_KEY, finalUID)` should use `setString`.
- `_api/identity.js:127` — `localStorage.setItem(KEY, u)` in `setUID` should use `setString`.
- `_api/identity.js:128` — `localStorage.setItem(ALIAS_KEY, u)` should use `setString`.
- `_api/identity.js:129` — `localStorage.setItem(LEGACY_KEY, u)` should use `setString`.
- `public/lux-popover.js:5` — `localStorage.getItem(KEY)` (public bundle reads UID directly, no helper).
- `features/harvard/modal-favs.js:9` — `localStorage.getItem(K_HARVARD_FAVS)` should use `getJSON`.
- `features/harvard/modal-favs.js:19` — `localStorage.getItem(K_PASSAGES_FAVS)` should use `getJSON`.
- `features/harvard/modal-favs.js:33` — `localStorage.setItem(K_HARVARD_FAVS, JSON.stringify(...))` should use `setJSON`.
- `features/harvard/modal-favs.js:36` — `localStorage.setItem(K_PASSAGES_FAVS, ...)` should use `setJSON`.
- `features/streaming/transport/session-bootstrap.js:13` — `localStorage.getItem(K_ADMIN_TOKEN)` should use `getString`.
- `features/streaming/transport/session-bootstrap.js:21` — `localStorage.setItem(K_ADMIN_TOKEN, t)` should use `setString`.

### 2. Silent catches without warnSwallow
- `ui/warp-nav.js:19` — `catch { return; }` — URL parse failure silently aborts warp.
- `ui/components/trouble-chips.js:168` — `catch (_) { /* safe */ }` — comment-only body.
- `features/interactions/boot.js:15` — `catch (e) { // swallow and try next }` — comment-only.
- `features/my-words/service.js:12` — `catch { return null; }` — silent fallback.
- `features/my-words/store.js:10` — `catch { return fallback; }` — silent fallback.
- `features/harvard/index.js:22` — `catch { return fallback; }` — silent fallback.
- `features/harvard/index.js:44` — `catch { return []; }` — silent fallback.
- `features/harvard/modal-favs.js:14` — `catch { favs = new Set(); }` — silent fallback.
- `features/harvard/modal-favs.js:24` — `catch { favKeys = new Set(); }` — silent fallback.
- `features/next-activity/next-practice.js:97` — `catch { return key; }` — silent fallback.
- `features/progress/wordcloud/side-drawers.js:11` — `catch { return fallback; }` — silent fallback.
- `features/recorder/audio-mode-core.js:51` — `catch { return AUDIO_MODES.NORMAL; }` — silent fallback.
- `features/recorder/audio-mode-core.js:64` — `catch { // ignore storage failures }` — comment-only.
- `features/recorder/audio-mode.js:52` — `catch { return {}; }` — silent fallback.
- `features/results/header.js:318` — `catch { return false; }` — silent sessionStorage read fallback.
- `features/streaming/transport/realtime-webrtc/mic-meter.js:53` — `catch { // never fail connect... }` — comment-only.
- `features/streaming/transport/realtime-webrtc/message-handler.js:37` — `catch { return; }` — silent JSON parse abort.
- `features/convo/picker-deck/cefr-hint-badge.js:67` — `catch { return "B1"; }` — silent default.
- `features/interactions/ph-hover/tooltip-video.js:106` — `catch (_) { // Fallback: browsers may block sound }` — comment-only (nested try with warnSwallow follows).
- `features/interactions/ph-hover/tooltip-modal.js:75` — `catch (_) { ... }` — nested try with warnSwallow follows, but outer body has no warn.

### 3. Broken imports
All 27 are stale dynamic imports in `_api/*.test.js` left over from the March 2026 `api/` → `_api/` rename. The test files themselves live in `_api/`, so `../api/*.js` resolves to a sibling directory that does not exist — these tests will throw `Cannot find module` at runtime.

- `_api/attempts.test.js:17` — `await import("../api/attempts.js")` — should be `./attempts.js`.
- `_api/identity.test.js:48` — `await import("../api/identity.js")` — should be `./identity.js`.
- `_api/identity.test.js:54` — `await import("../api/identity.js")`.
- `_api/identity.test.js:62` — `await import("../api/identity.js")`.
- `_api/identity.test.js:68` — `await import("../api/identity.js")`.
- `_api/identity.test.js:77` — `await import("../api/identity.js")`.
- `_api/identity.test.js:86` — `await import("../api/identity.js")`.
- `_api/identity.test.js:96` — `await import("../api/identity.js")`.
- `_api/identity.test.js:106` — `await import("../api/identity.js")`.
- `_api/identity.test.js:112` — `await import("../api/identity.js")`.
- `_api/identity.test.js:122` — `await import("../api/identity.js")`.
- `_api/identity.test.js:128` — `await import("../api/identity.js")`.
- `_api/identity.test.js:137` — `await import("../api/identity.js")`.
- `_api/identity.test.js:145` — `await import("../api/identity.js")`.
- `_api/util.test.js:50` — `await import("../api/util.js")` — should be `./util.js`.
- `_api/util.test.js:60` — `await import("../api/util.js")`.
- `_api/util.test.js:73` — `await import("../api/util.js")`.
- `_api/util.test.js:85` — `await import("../api/util.js")`.
- `_api/util.test.js:95` — `await import("../api/util.js")`.
- `_api/util.test.js:102` — `await import("../api/util.js")`.
- `_api/util.test.js:111` — `await import("../api/util.js")`.
- `_api/util.test.js:120` — `await import("../api/util.js")`.
- `_api/util.test.js:129` — `await import("../api/util.js")`.
- `_api/util.test.js:140` — `await import("../api/util.js")`.
- `_api/util.test.js:154` — `await import("../api/util.js")`.
- `_api/util.test.js:161` — `await import("../api/util.js")`.
- `_api/util.test.js:172` — `await import("../api/util.js")`.

No commented-out import statements were found. The two `// import ...` matches (`app-core/lux-bus.js:7`, `app-core/lux-listeners.js:7`) are usage-example lines inside header docblocks, not disabled code.

### 4. API path drift
Same-origin `/api/...` references that should be `/_api/...` per the March 2026 Vercel Hobby rename. `${API_BASE}/api/...` calls are NOT flagged — `API_BASE` points at the separate backend Vercel project where `api/` still lives.

- `_api/util.test.js:51` — `apiFetch("/api/test")` — should be `/_api/test`.
- `_api/util.test.js:61` — `apiFetch("/api/test", ...)`.
- `_api/util.test.js:76` — `apiFetch("/api/upload", ...)`.
- `_api/util.test.js:86` — `apiFetch("/api/data")`.
- `_api/util.test.js:96` — `apiFetch("/api/missing")`.
- `_api/util.test.js:103` — `apiFetch("/api/text", ...)`.
- `_api/util.test.js:112` — `apiFetch("/api/audio", ...)`.
- `_api/util.test.js:121` — `apiFetch("/api/raw", ...)`.
- `_api/util.test.js:130` — `apiFetch("/api/cancel", ...)`.
- `_api/util.test.js:141` — `apiFetch("/api/custom", ...)`.
- `_api/alt-meaning.js:2` — stale header comment `calls backend /api/router?route=alt-meaning` (line 6 was updated to `/_api/router?...` on 4/15; comment was not).
- `_api/util.js:9` — in-code comment `// ✅ Dev: default to same-origin so "/api/..." hits Vite proxy`.
- `_api/util.js:75` — usage-example comment `apiFetch("/api/some-route", ...)`.
- `index.html:35` — importmap entry `"./api.js": "/src/_api/index.js"` references `/src/_api/index.js` which does not exist (should be `/_api/index.js`). No code currently imports `./api.js`, so this is dead config rather than a runtime break, but it is drift against the rename.

### 5. Missing mounts
All 14 exported `mount*` functions are imported at least once by a consumer. Cross-reference complete:

- `mountAICoachAlwaysOn` — consumed by `features/dashboard/index.js`, `features/convo/convo-ai-coach-shell.js`, `features/recorder/index.js`.
- `mountAudioModeSwitch` — `src/main.js`, `features/convo/convo-bootstrap.js`, `features/recorder/index.js`.
- `mountCefrHintBadge` — `features/convo/picker-deck/deck-card.js`.
- `mountKnobsDrawer` — consumed internally via `getKnobsDrawerInstance()` factory (exported + imported by `convo-modes.js`, `convo-knobs-ui.js`, `convo-picker-system.js`).
- `mountLifeApp` — `src/life.js`.
- `mountMyWordsCornerLauncher` — `features/my-words/index.js`, `features/my-words/boot.js`.
- `mountMyWordsPanel` — `features/my-words/library-modal.js`, `features/my-words/index.js`, `features/my-words/library-modal-controller.js` (re-exported as `mountPanel`).
- `mountSelfPlaybackLite` — `features/features/08-selfpb-peekaboo.js` (via dynamic module.mountSelfPlaybackLite).
- `mountStreamingApp` — `src/stream.js`.
- `mountStreamingSetupApp` — `src/stream-setup.js`.
- `mountSyllablesForTable` — `features/interactions/score-collapse.js`, `features/results/render-core.js` (via dynamic module).
- `mountTTSPlayer` — `features/features/tts/boot-tts.js` (dynamic).
- `mountVoiceMirrorButton` — `features/results/summary.js`, `features/features/tts/player-ui.js`.

✅ Clean.

## Notes
- `docs/ARCHITECTURE.md` still references `api/` (not `_api/`) throughout — lines 105, 240, 246, 249, 264–271, 315, 324, 325. Documentation drift from the March 2026 rename.
- `app-core/lux-storage.js` and `ui/lux-warn.js` have internal bare `catch {}` / `catch (e) {}` blocks by design (canonical helpers that cannot call themselves recursively). These are excluded from the silent-catch tally.
- `app-core/lux-storage.test.js:141,142,147,151,152,157` contain `globalThis.localStorage.*` references, but these are test-mock reassignments (not storage access). Excluded from the localStorage tally.
- `features/recorder/audio-inspector.js:4` matches the localStorage regex but is a comment ("or in console: localStorage.setItem..."). Excluded.
- The two commits in the 24-hour window (`699f55b`, `9b1a430`) only patched the production same-origin `/api/` usage (alt-meaning) and an archived docs file. The stale comments and all 27 broken test imports remain — they appear to have been missed during the same-origin rename sweep.
