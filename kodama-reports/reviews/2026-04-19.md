# Deep Code Review — 2026-04-19

## Summary
Files reviewed: 2
CRITICAL: 0 | WARNING: 0 | INFO: 3

## Top Investigation
No investigation needed — everything flagged is advisory. The recent changes (XSS fix + warnSwallow guards) are small, correct, and defensive.

## Findings

### CRITICAL
✅ None

### WARNING
✅ None

### INFO

- `features/results/summary.js:160` — `fmtPctCefr(err.score)` is interpolated into the `title="..."` attribute without escaping. `err.score` is a number from Azure's `AccuracyScore` so in practice this is safe, but if `fmtPctCefr` ever returns a string containing `"` the attribute would break out. One-liner fix if you care: wrap with `escapeHtml(...)`.

- `features/results/summary.js:120` — `youtubeId` is interpolated directly into `src="https://www.youtube.com/embed/${youtubeId}"` without URL encoding. Currently sourced from the static `passages.js` data file (trusted), but worth a comment there noting "values must be URL-safe IDs" so a future contributor who adds a passage doesn't paste a URL with query params / quote chars.

- `features/harvard/modal-actions.js:96,133` — `globalThis.warnSwallow?.(...)` silently no-ops when the helper is missing. That's the point of the guard, but it does mean a bootstrap ordering bug (modal fires before `warnSwallow` registers) would now go completely unnoticed. Low confidence this matters — if `warnSwallow` is mounted early in the boot sequence this is fine.

## Files Reviewed

- `features/harvard/modal-actions.js` (257 lines) — added `?.` optional chaining on two `globalThis.warnSwallow` calls inside catch blocks around `setString` persistence. Defensive, reasonable.
- `features/results/summary.js` (183 lines) — imported `escapeHtml`, wrapped `err.word` in the "Words to Review" pill render. Directly addresses a real reflected-data path (`err.word` originates from Azure's word recognition and could contain quote chars). Good fix.

## Notes

Both diffs are tight and well-scoped. I intentionally did not flag:
- The redundant `isCorrupt` filter at `summary.js:79` (already filtered during aggregation at line 50) — pre-existing, not touched by this change.
- `err.score` / `err.part` interpolation — those are numbers from a trusted pipeline.
- `$out.innerHTML = html` at line 172 — the `html` string is composed from static copy + escaped word + numeric scores, so post-fix it's clean.

Confidence: high on the summary.js observations (I read the full template), medium on the warnSwallow observability note (depends on boot ordering I didn't trace).
