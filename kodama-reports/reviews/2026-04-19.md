# Deep Code Review — 2026-04-19

## Summary
Files reviewed: 2
CRITICAL: 0 | WARNING: 1 | INFO: 2

## Top Investigation
No investigation needed — everything flagged is advisory; the recent XSS fix in `summary.js` is sound, and the `warnSwallow?.` guards in `modal-actions.js` are defensive improvements.

## Findings

### CRITICAL
✅ None

### WARNING
- `features/results/summary.js:160` — `title="${fmtPctCefr(err.score)}"` interpolates without escaping; `err.score` is numeric so currently safe, but if `fmtPctCefr` ever returns a string containing `"`, it would break out of the title attribute. Low-confidence — depends on `fmtPctCefr` runtime contract; consider escaping for symmetry with the new `escapeHtml(err.word)` fix on line 159.

### INFO
- `features/results/summary.js:79` — `worstErrors = majorIssues.filter((e) => !isCorrupt(e.word)).slice(0, 5)` re-filters `majorIssues`, but line 50 already early-returns on `isCorrupt(w.Word)` before pushing into `majorIssues`. The filter is dead-weight (harmless, but signals stale code).
- `features/harvard/modal-actions.js:84,91,122,128` — outside the changed lines, but adjacent: `selTitle.textContent`, `practiceBtn.textContent`, and `practiceBtn.disabled` are accessed without null guards (only `listEl` is checked at the top). If any of those DOM refs are missing, the function will throw *before* reaching the new `warnSwallow?.` guard, defeating its purpose. Pre-existing, flagging for awareness only.

## Files Reviewed
- `features/results/summary.js` (183 lines) — XSS hardening: imported `escapeHtml` and wrapped `err.word` interpolation. The fix is the right shape; one adjacent attribute interpolation is the only residual concern.
- `features/harvard/modal-actions.js` (257 lines) — Defensive: switched two `globalThis.warnSwallow(...)` calls to optional chaining. Correct fix for environments where the global isn't yet installed.

## Notes
Very thin diff window (4 insertions, 3 deletions across 2 files). Both changes are narrow defensive fixes — no behavioral changes to review beyond the immediate XSS-hardening and optional-chaining patches. Confidence is high on the changes themselves; the WARNING is low-confidence because it depends on `fmtPctCefr`'s string contract, which I did not open. Nothing in the diff hints at a logic bug a regex couldn't catch.
