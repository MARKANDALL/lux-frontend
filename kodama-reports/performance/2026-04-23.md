# Performance Budget Check — 2026-04-23

## Summary
- Oversized JS files: 5 (4 are cohesive domain/data files — low concern)
- Oversized CSS files: 2 (both authored, none are generated rollups)
- Eager heavy imports: 0
- Duplicate utility candidates: 3
- Unoptimized images: 14

## Findings

### 1. Oversized JS files (>500 lines)

| Lines | File | Purpose | Severity |
|---|---|---|---|
| 4362 | `src/data/harvard-phoneme-meta.js` | Auto-generated phoneme metadata (`// AUTO-GENERATED by scripts/build-harvard-phonemes.mjs`). Cohesive data module — do not split. | LOW |
| 3689 | `src/data/passage-phoneme-meta.js` | Auto-generated passage phoneme metadata (`// AUTO-GENERATED by scripts/build-passage-phonemes.mjs`). Cohesive data module — do not split. | LOW |
| 939 | `src/data/harvard-lists.js` | Static Harvard Sentences dataset (72 lists × 10 sentences). Cohesive data module. | LOW |
| 657 | `features/convo/scenarios.js` | Source-of-truth list of 25 AI conversation scenarios (title + desc + roles). Cohesive data module. | LOW |
| 505 | `public/vendor/d3.layout.cloud.js` | Vendored 3rd-party d3-cloud library (Jason Davies). Loaded lazily only on the Word Cloud page — no splitting needed. | LOW |

Note: All five are either data/registry modules or vendored libs. None are control-flow modules that would benefit from being split. Fixing would not meaningfully improve initial load (these are already route-specific or tree-shakeable imports).

### 2. Oversized CSS files (>800 lines)

| Lines | File | Note | Severity |
|---|---|---|---|
| 964 | `lux-progress.css` | Authored, single-concern (`/* lux-progress.css — user-first progress UI */`). Only loaded on Progress page. Split would be cosmetic. | LOW |
| 948 | `features/features/tts.css` | Authored, single-concern (TTS drawers + Self-Playback quadrant). Feature-scoped. | LOW |

No generated rollup / `*COMBINED*` CSS files detected at the repo root or under `public/`.

### 3. Eager heavy imports

None detected.

Heavy libraries checked and their load strategy:
- **WaveSurfer.js** — loaded via dynamic `<script>` tag in `features/features/08-selfpb-peekaboo.js:124` only when the Self-Playback drawer is clicked. ✅ Lazy.
- **d3 + d3-cloud** — loaded via `ensureScript()` in `features/progress/wordcloud/libs.js:26-27`, only when the Word Cloud page opens. ✅ Lazy.
- **Three.js / Plotly / TensorFlow(.js)** — no imports anywhere in the codebase.

`src/main.js` has 18 top-level imports (below the 20-module threshold). Entry files for other pages have 3–9 imports each. Nothing to flag.

### 4. Duplicate utility candidates (3+ definitions)

| Name | Files | Candidate action |
|---|---|---|
| `showHoverHarvard` / `showHoverPassage` / `positionHoverCard` (3-function cluster) | `features/harvard/modal-actions.js:136,156,180`, `features/harvard/modal-controller.js:241,245,251`, `features/harvard/modal-controller/interaction-handlers.js:49,58,71` | Same hover-card logic redefined in three Harvard-modal files. Consolidate into the `modal-controller/interaction-handlers.js` version and have the other two import from it. **MEDIUM** — three copies increase drift risk and add bytes to any bundle that loads more than one. |
| `getLuxUID` | `features/results/summary-shell.js:14`, `features/voice-mirror/voice-onboarding.js:155`, `features/voice-mirror/voice-mirror.js:18` | Three independent re-implementations of the same UID resolver. Export once from `_api/identity.js` (already exposes `ensureUID`/`getUID`) and delete the local copies. **LOW–MEDIUM**. |
| `mean` + `getColorConfig` (co-located in progress/convo helpers) | `mean`: `features/interactions/metric-modal/derive/utils.js:11`, `features/progress/progress-utils.js:17`, `features/convo/convo-report-ui.js:17`. `getColorConfig`: `features/progress/progress-utils.js:9`, `features/convo/convo-report-ui.js:23`, `features/results/summary.js:17` | Same numeric helpers duplicated across progress/convo/results. Candidate for extraction into a shared `helpers/stats.js` or into `helpers/core.js`. **LOW**. |

No occurrences of `escapeHtml`/`escapeHTML`, `clamp`, `debounce`, `throttle`, `formatDate` defined in 3+ files today. `escapeHtml` lives canonically in `helpers/escape-html.js` (1 def), and `clamp` has only 2 defs (`helpers/core.js:37` + `features/harvard/index.js:58`) — worth deduping but below the 3-file threshold.

### 5. Unoptimized images (>200 KB under `public/`)

14 raw `.jpg` character portraits. All under `public/assets/characters/` except one in `public/convo-img/`. These are rendered in the AI-conversation character picker.

| Size | File |
|---|---|
| 485 KB | `public/assets/characters/joke-joker.jpg` |
| 480 KB | `public/assets/characters/hiking-guide.jpg` |
| 390 KB | `public/assets/characters/videocall-listener.jpg` |
| 360 KB | `public/assets/characters/networking-newcomer.jpg` |
| 350 KB | `public/assets/characters/technology-user.jpg` |
| 340 KB | `public/assets/characters/restaurant-diner.jpg` |
| 328 KB | `public/assets/characters/understanding-confused.jpg` |
| 319 KB | `public/assets/characters/shopping-shopper.jpg` |
| 300 KB | `public/assets/characters/calling-receiver.jpg` |
| 292 KB | `public/assets/characters/airport-agent.jpg` |
| 255 KB | `public/assets/characters/lifeguard-visitor.jpg` |
| 241 KB | `public/assets/characters/student-teacher.jpg` |
| 237 KB | `public/convo-img/mail.jpg` |
| 211 KB | `public/assets/characters/shopping-associate.jpg` |

Aggregate: ~4.7 MB of heavy `.jpg` in `public/assets/characters/` (of 50 portraits total). 47 `.webp` files already exist in `public/` — conversion pipeline appears partial. **MEDIUM** — converting these 14 to WebP (or adding `srcset` with smaller variants) would meaningfully reduce the payload on any page that lazy-loads a character grid.

## Notes

- First performance-budget report under `kodama-reports/performance/` — no prior report to trend against. Future weekly runs should compare oversized-file counts and new heavy imports vs. this baseline.
- Trend-to-watch: the two 500-line+ CSS files (`lux-progress.css`, `features/features/tts.css`) — if they keep growing week over week, consider splitting. Today both are cohesive.
- Lazy-loading discipline is strong: every heavy 3rd-party library (WaveSurfer, d3, d3-cloud) is gated behind a user action or route. No regressions to flag.
- Out-of-scope observation: `hero.png` at repo root is 326 KB and referenced from `index.html`. Worth considering for WebP conversion if it lives on the critical initial-paint path, but it sits outside the `public/` scope of this check.
