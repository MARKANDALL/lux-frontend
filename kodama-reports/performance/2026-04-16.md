# Performance Budget Check — 2026-04-16

Static analysis only. No lighthouse runs, bundle builds, or network requests were executed.

Scope: `.js`, `.jsx`, `.css`, `.html` files under the repo root.
Excluded: `node_modules/`, `dist/`, `_ARCHIVE/`, `_agents-archive/`, `_parts/`, and any other underscore-prefixed folder (except `_api/`).

## Summary
- Oversized JS files: 5
- Oversized CSS files: 2 (authored); 0 generated rollups detected
- Eager heavy imports: 0
- Duplicate utility candidates: 1
- Unoptimized images: 14

## Findings

### 1. Oversized JS files (>500 lines)

| Lines | File | Purpose | Severity |
| ----: | ---- | ------- | :------: |
| 4362 | `src/data/harvard-phoneme-meta.js` | AUTO-GENERATED phoneme metadata for Harvard sentences (header: "AUTO-GENERATED by scripts/build-harvard-phonemes.mjs — Do not hand-edit"). Cohesive data file. | LOW |
| 3689 | `src/data/passage-phoneme-meta.js` | AUTO-GENERATED phoneme metadata for passages (header: "AUTO-GENERATED by scripts/build-passage-phonemes.mjs — Do not hand-edit"). Cohesive data file. | LOW |
| 939 | `src/data/harvard-lists.js` | Source data: 72 Harvard Sentence lists × 10 sentences each. Cohesive domain data. | LOW |
| 657 | `features/convo/scenarios.js` | Source of truth for all 25 AI conversation scenarios (title/desc/bullets/roles). Cohesive domain data. | LOW |
| 505 | `public/vendor/d3.layout.cloud.js` | Vendored third-party library (`d3-cloud` by Jason Davies). Not authored. | LOW |

All five are either auto-generated data, cohesive domain data, or vendored third-party code. No splits recommended — fixing would not meaningfully improve initial load time because none of these files are loaded on the Practice Skills / home page boot path. `harvard-*` data is used by the Harvard feature; `scenarios.js` is used by Conversations; `d3.layout.cloud.js` is lazy-injected by `features/progress/wordcloud/libs.js`.

### 2. Oversized CSS files (>800 lines)

| Lines | File | Authored / Rollup | Severity |
| ----: | ---- | ----------------- | :------: |
| 964 | `lux-progress.css` | Authored — user-first progress UI styles. | LOW |
| 948 | `features/features/tts.css` | Authored — Lux TTS mini drawer + Self Playback quadrant. | LOW |

**Note on rollups:** No generated rollup/combined CSS files were found in the active source tree (no `*COMBINED*`, `*.combined.*`, or `*.rollup.*` under the live paths). The prior-era rollup pair (`_ARCHIVE/legacy-hygiene/_lux-convo.concat.css`, `_ARCHIVE/legacy-hygiene/lux-convo.full.css`) is archived and out of scope. No `lux-layout.COMBINED.css` exists today. Flagging absence as informational, not drift.

Both oversized authored files are within a reasonable ceiling for feature-complete UI subsystems. Splitting would not meaningfully reduce CSS payload because both are scoped to their respective features.

### 3. Eager imports of heavy modules

None detected.

- `wavesurfer.js` is loaded lazily by `features/features/08-selfpb-peekaboo.js:124` via dynamic script injection (`/vendor/wavesurfer-7.8.11.min.js`) on the Self-Playback tab click. The wrapper `features/features/selfpb/waveform-logic.js` only touches `window.WaveSurfer` inside function bodies.
- `three`, `plotly`, and `tensorflow` / `tensorflowjs`: no source references found.
- `d3` / `d3.layout.cloud`: loaded lazily via `ensureScript(...)` in `features/progress/wordcloud/libs.js:23-27`. No ES `import` from `"d3"` anywhere in the codebase.
- `src/main.js` has **18 top-level static imports** (below the 20+ threshold). Several are already lazy (`08-selfpb-peekaboo.js` is itself a lazy loader). Other entry files (`src/convo.js`, `src/life.js`, `src/progress.js`, `src/stream*.js`, `src/wordcloud.js`) each have ≤9 static imports.

### 4. Duplicate utility function candidates

| Function | Files | Severity |
| -------- | ----- | :------: |
| `mdToHtml` | `helpers/md-to-html.js:12`, `helpers/core.js:39`, `features/progress/progress-utils.js:18` | LOW |

`mdToHtml` is defined (not just imported) in three separate files. `helpers/md-to-html.js` is the richer variant (accepts an `options` argument). The `helpers/core.js` copy and the `features/progress/progress-utils.js` copy are simpler fallbacks that duplicate the same markdown-lite → HTML behavior. Consolidating on `helpers/md-to-html.js` would remove ~40 lines of duplicate logic but won't measurably move the bundle needle — flagged for cleanup rather than perf.

Near-misses (defined in exactly 2 files — **not** flagged per the 3+ rule, but recorded here so next week can watch for drift):
- `clamp` — `helpers/core.js:50`, `features/harvard/index.js:58`
- `escapeHtml` vs `escapeHTML` — `helpers/escape-html.js:4`, `features/interactions/ph-hover/utils.js:4` (different casing/behaviour)
- `pad2` — `src/data/harvard-key.js:4`, `src/data/harvard.js:6` (plus `scripts/build-harvard-phonemes.mjs:7`, but `.mjs` is out of scope)
- `fmtPct` — `core/scoring/index.js:7`, `features/progress/render/dashboard/actions-and-trends.js:55`
- `safeParse` — `features/my-words/store.js:7`, `features/progress/wordcloud/side-drawers.js:6`
- `shuffleInPlace` — `helpers/core.js:55`, `features/harvard/index.js:104`

### 5. Unoptimized images (.png/.jpg under `public/` >200KB)

All 14 offenders are `.jpg` character portraits for Conversations (no oversized `.png` files found). Sizes rounded:

| KB | File |
| --: | ---- |
| 486 | `public/assets/characters/joke-joker.jpg` |
| 481 | `public/assets/characters/hiking-guide.jpg` |
| 390 | `public/assets/characters/videocall-listener.jpg` |
| 360 | `public/assets/characters/networking-newcomer.jpg` |
| 351 | `public/assets/characters/technology-user.jpg` |
| 341 | `public/assets/characters/restaurant-diner.jpg` |
| 329 | `public/assets/characters/understanding-confused.jpg` |
| 319 | `public/assets/characters/shopping-shopper.jpg` |
| 301 | `public/assets/characters/calling-receiver.jpg` |
| 293 | `public/assets/characters/airport-agent.jpg` |
| 256 | `public/assets/characters/lifeguard-visitor.jpg` |
| 242 | `public/assets/characters/student-teacher.jpg` |
| 238 | `public/convo-img/mail.jpg` |
| 211 | `public/assets/characters/shopping-associate.jpg` |

Total: ~4.6 MB across 14 files. Severity **MEDIUM** — converting character portraits to WebP typically yields 40–70% size reduction with similar visual quality, and these load on the Conversations character picker. 47 `.webp` files already exist in `public/`, so the pipeline is partly in place; character portraits are the stragglers.

## Notes

- **First report in `kodama-reports/performance/`.** No prior report in this directory, so no week-over-week trend yet. Earlier weekly findings live in `_agents-archive/kodama/WEEK-{1,2,3}-FINDINGS.md` but use a different format.
- **Encouraging signals this week:** every heavy library (WaveSurfer, d3, d3-cloud) is already lazy-loaded behind a user action. No blocking heavy module sits on the initial-load path.
- **Watchlist for next week:**
  - If `src/main.js` top-level imports cross 20, reconsider splitting the onboarding/dashboard/auth boot sequence behind `import()`.
  - If another file defines `clamp` or `escapeHtml`, duplicate count crosses the 3-file threshold.
  - Trendline on `public/assets/characters/` total size if more scenarios are added.
