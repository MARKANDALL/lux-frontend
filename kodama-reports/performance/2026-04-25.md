# Performance Budget Check — 2026-04-25

## Summary
- Oversized JS files: 6 (3 are auto-generated data; 1 is a vendor bundle)
- Oversized CSS files: 2 (no generated rollups present)
- Eager heavy imports: 0
- Duplicate utility candidates: 1 (low impact)
- Unoptimized images: 14

## Findings

### 1. Oversized JS files (>500 lines)

| Lines | File | Notes |
|---:|---|---|
| 4362 | `src/data/harvard-phoneme-meta.js` | Auto-generated phoneme metadata (`scripts/build-harvard-phonemes.mjs`). Cohesive data file — do not split. |
| 3689 | `src/data/passage-phoneme-meta.js` | Auto-generated passage phoneme metadata. Cohesive data file. |
| 939 | `src/data/harvard-lists.js` | Authored corpus: 72 lists × 10 Harvard sentences. Cohesive — data file. |
| 658 | `features/convo/scenarios.js` | Source-of-truth for all 25 AI conversation scenarios (titles, descriptions, roles). Cohesive content/config. |
| 637 | `features/voice-mirror/voice-mirror.js` | "Hear it in my voice" tile + custom audio player for the TTS drawer. Single feature module. **Borderline** — could be split into UI vs. API client if it grows further. Severity: LOW. |
| 505 | `public/vendor/d3.layout.cloud.js` | Vendor library (d3 word-cloud layout). Not authored. Already loaded lazily via `ensureScript` in `features/progress/wordcloud/libs.js:27`. |

Self-check: none of these meaningfully drive initial load — the data files are imported only by feature paths that need them, the vendor file is lazy, and `voice-mirror.js` is feature-scoped.

### 2. Oversized CSS files (>800 lines)

| Lines | File | Notes |
|---:|---|---|
| 964 | `lux-progress.css` | Authored. "user-first progress UI (clean → expandable)". One large stylesheet for the entire progress page. **Borderline** — could be sectioned (shell / accordion / charts) if it keeps growing. Severity: LOW. |
| 948 | `features/features/tts.css` | Authored. Mini TTS drawers + expanded Self Playback TTS. Single-feature stylesheet; cohesive. Severity: LOW. |

No `*COMBINED*.css` rollup files exist in this tree — nothing to flag separately.

### 3. Eager imports of heavy modules

None.

- **wavesurfer**: only present as `public/vendor/wavesurfer-7.8.11.min.js`, loaded on demand via `ensureScript("/vendor/wavesurfer-7.8.11.min.js", ...)` in `features/features/08-selfpb-peekaboo.js:124`. Already lazy.
- **d3 / d3-cloud**: loaded via `ensureScript("/vendor/d3.v7.min.js", ...)` in `features/progress/wordcloud/libs.js:26-27`. Already lazy.
- **three / Plotly / tensorflow**: not present in the repo.
- `src/main.js` has 18 top-level imports (under the 20+ threshold). Other entry-point `index.js` files: ≤9.

### 4. Duplicate utility function candidates

Across in-tree code (excluding `node_modules/`, `_ARCHIVE/`, `_agents-archive/`, `public/vendor/`):

| Name | Files | Status |
|---|---|---|
| `clamp` | `helpers/core.js:37` (exported), `features/harvard/index.js:58` (local) | 2 files — under the 3-file threshold. Borderline; harvard's local copy could re-use the helper. Severity: LOW. |
| `fmt` | `features/results/summary-shell.js:50`, `public/lux-popover.js:31`, `features/features/selfpb/core.js:17` | 3 files. Two of them are the **same date formatter** (`(d) => d.toISOString().slice(0,10)`); the third formats time. Consolidating the date formatter into a helper would dedupe ~3 lines — marginal. Severity: LOW. |
| `escapeHtml`, `debounce`, `throttle`, `formatDate` | only one definition each | No dup. |

### 5. Unoptimized images (.png/.jpg under `public/` >200 KB)

14 files, all `.jpg`, totalling ~4.6 MB:

| Size | File |
|---:|---|
| 497 KB | `public/assets/characters/joke-joker.jpg` |
| 492 KB | `public/assets/characters/hiking-guide.jpg` |
| 400 KB | `public/assets/characters/videocall-listener.jpg` |
| 369 KB | `public/assets/characters/networking-newcomer.jpg` |
| 359 KB | `public/assets/characters/technology-user.jpg` |
| 349 KB | `public/assets/characters/restaurant-diner.jpg` |
| 337 KB | `public/assets/characters/understanding-confused.jpg` |
| 327 KB | `public/assets/characters/shopping-shopper.jpg` |
| 308 KB | `public/assets/characters/calling-receiver.jpg` |
| 300 KB | `public/assets/characters/airport-agent.jpg` |
| 262 KB | `public/assets/characters/lifeguard-visitor.jpg` |
| 247 KB | `public/assets/characters/student-teacher.jpg` |
| 243 KB | `public/convo-img/mail.jpg` |
| 216 KB | `public/assets/characters/shopping-associate.jpg` |

Notes:
- `public/convo-img/` already mostly serves WebP thumbs (`public/convo-img/thumbs/*.webp`, plus webp at the top level for most scenarios). A handful of scenarios still ship `.jpg` originals at the top level (`car.jpg`, `joke.jpg`, `mail.jpg`, `party.jpg`); only `mail.jpg` exceeds 200 KB. Worth converting.
- `public/assets/characters/` is the bulk of the unoptimized weight — 13 character JPGs. WebP @ ~q80 typically yields a 30–50% reduction here. Severity: MEDIUM (these are user-facing images on the convo flow; not all loaded at once, but several on the initial scenario picker).
- 47 WebP files already exist in `public/`, so the toolchain/pattern is clearly established.

## Notes

Trend vs. prior reports (issue #41 comments):

| Check | 04-16 | 04-18 | 04-23 | **04-25** | Δ vs. last |
|---|---:|---:|---:|---:|---|
| Oversized JS (>500L) | 5 | 5 | 5 | **6** | **+1 — `features/voice-mirror/voice-mirror.js` crossed the threshold (now 637L)** |
| Oversized CSS (>800L) | 2 | 2 | 2 | **2** | flat |
| Eager heavy imports | 0 | 0 | 0 | **0** | flat |
| Duplicate utilities | 1 | 4 | 3 | **1** | -2 (prior `mdToHtml`/`mean`/`getColorConfig`/`getLuxUID` clusters not re-detected this run with the strict 3+ same-name rule) |
| Unoptimized images | 14 | 14 | 14 | **14** | flat (~4.6 MB) |

- **New this week:** `features/voice-mirror/voice-mirror.js` grew past 500 lines (the file was modified this week per `git log --since="7 days ago"`). Severity LOW for now — single cohesive feature — but worth watching if it keeps growing.
- The week's commits also added two character JPGs (`party-host.jpg`, `party-newcomer.jpg`) and a `party.mp4`, plus a sibling `convo-img/thumbs/party.webp`. Neither new JPG exceeds 200 KB so they don't appear in the unoptimized list.
- Top recommended action (highest impact for least effort, unchanged from prior weeks): convert the 13 character JPGs in `public/assets/characters/` to WebP. Estimated savings: ~1.5–2 MB. WebP pipeline is already partially established (47 WebP files exist in `public/`).
- Low-priority follow-ups: dedupe the `fmt` date formatter (helpers candidate); consider whether `voice-mirror.js` should split if it crosses ~800 lines.
