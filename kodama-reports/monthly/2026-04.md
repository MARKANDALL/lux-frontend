# Monthly Hygiene Sweep — April 2026

**Repo:** `markandall/lux-frontend`
**Date:** 2026-04-16
**Scope:** Full repo excluding `_ARCHIVE/`, `_agents-archive/`, `node_modules/`, `dist/`, underscore folders (except `_api/`).

---

## 1. .GOLD Backup Cleanup Candidates

**Count: 0**

No `.GOLD` or `.GOLD.N` files found anywhere in `lux-frontend`.

> **Tracking note:** The previous count of 19 `.GOLD` files (as of 2026-04-15) was in the `luxury-language-api` repo, not this one. This repo has held at **0** — no growth to report.

Nothing to clean up.

---

## 2. Unused Dependencies

| Package | Type | Status | Notes |
|---|---|---|---|
| `eslint` ^8.57.1 | devDep | Used | `.eslintrc.json` config + `npm run lint` |
| `serve` 11.2.0 | devDep | **Removal candidate** | Not imported anywhere. Not referenced in any npm script. No usage in config files or docs. |
| `sharp` ^0.34.5 | devDep | Used | `scripts/make-thumbs.mjs` |
| `vite` ^6.0.0 | devDep | Used | `vite.config.js`, `npm run dev/build/preview` |
| `vitest` ^4.0.18 | devDep | Used | 7 test files across `_api/`, `app-core/`, `features/` |
| `@supabase/supabase-js` ^2.87.1 | dep | Used | `src/supabase.js` |
| `cmu-pronouncing-dictionary` ^3.0.0 | dep | Used | `features/results/syllables/cmu-stress.js`, build scripts |

**Summary:** 1 removal candidate (`serve`).

`serve` appears to be a leftover from a pre-Vite era when a static file server was needed. Vite's `npm run preview` now fills that role. Safe to remove when ready.

---

## 3. Stale Branches

| Branch | Last Commit | Age | Status |
|---|---|---|---|
| `main` | 2026-04-15 | 1 day | Active |
| `claude/zealous-johnson-fpvGc` | 2026-04-15 | 1 day | Active (this sweep) |

**Summary:** 0 stale branches. Only 2 branches exist, both active within the last 24 hours.

Nothing to clean up.

---

## 4. Env Var Drift

**No `.env.example` file exists.** All env vars referenced in code are effectively undocumented for onboarding purposes.

### Env vars referenced in code (excluding Vite built-ins `DEV`/`PROD`)

| Variable | File(s) | Category |
|---|---|---|
| `VITE_SUPABASE_URL` | `src/supabase.js` | Supabase auth |
| `VITE_SUPABASE_ANON_KEY` | `src/supabase.js` | Supabase auth |
| `VITE_API_BASE` | `_api/util.js` | API routing |
| `LUX_API_ORIGIN` | `vite.config.js` | Dev proxy target |
| `VITE_LUX_API_ORIGIN` | `vite.config.js` | Dev proxy target (alt) |
| `VITE_ADMIN_TOKEN` | `vite.config.js` | Admin header injection |
| `VITE_LUX_ADMIN_TOKEN` | `vite.config.js` | Admin header injection (alt) |
| `LUX_ADMIN_TOKEN` | `vite.config.js` | Admin header injection (alt) |
| `ADMIN_TOKEN` | `vite.config.js` | Admin header injection (alt) |
| `PORT` | `tools/dev-realtime-proxy.mjs` | Realtime proxy |
| `OPENAI_API_KEY` | `tools/dev-realtime-proxy.mjs` | Realtime proxy |
| `OPENAI_REALTIME_MODEL` | `tools/dev-realtime-proxy.mjs` | Realtime proxy |
| `OPENAI_REALTIME_VOICE` | `tools/dev-realtime-proxy.mjs` | Realtime proxy |

**Total drift: 13 env vars** with no `.env.example` to document them.

The README (line 163) mentions `LUX_API_ORIGIN` and `VITE_LUX_API_ORIGIN` in prose, and references admin-token variables, but there is no machine-readable `.env.example` a new contributor could copy.

**Recommendation:** Create a `.env.example` with commented entries for all 13 vars. The Supabase and API origin vars are essential for local dev; the admin/realtime vars are optional.

---

## 5. README Accuracy

### Commands verified against `package.json` scripts

| README Command | Script Exists | Runs Successfully |
|---|---|---|
| `npm install` | N/A | Yes |
| `npm run dev` | `"dev": "vite"` | Yes |
| `npm run build` | `"build": "vite build"` | Yes (6s, sourcemaps, chunk warnings) |
| `npm run preview` | `"preview": "vite preview"` | Yes |
| `npm test` | `"test": "vitest run"` | Runs, but 27/69 tests fail (3 files) |
| `npm run test:watch` | `"test:watch": "vitest"` | Yes |
| `npm run lint` | `"lint": "eslint . --ext .js,.mjs"` | Yes (5 errors, 83 warnings) |
| `npm run hygiene` | `"hygiene": "..."` | Yes |

### Notes

- **Test failures are pre-existing**, not a README accuracy issue. All 27 failures are in `_api/` test files due to a module resolution path mismatch (`../api/util.js` vs `../_api/util.js`). The README does not claim tests pass — it just shows how to run them.
- **Page entry points** listed in README match `vite.config.js` rollup inputs exactly.
- **Env var documentation** in README prose is accurate but informal (see Section 4).
- **Prerequisites** (Node.js 18+, backend) are correctly stated.
- **No mismatches found** between README commands and actual package.json scripts.

---

## Summary

| Check | Finding | Action Items |
|---|---|---|
| .GOLD backups | 0 files | None |
| Unused deps | 1 candidate (`serve`) | Manual review for removal |
| Stale branches | 0 stale | None |
| Env var drift | 13 undocumented vars, no `.env.example` | Create `.env.example` |
| README accuracy | All commands valid | None |
