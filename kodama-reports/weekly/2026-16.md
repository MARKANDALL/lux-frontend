# Weekly Architecture Audit — 2026 Week 16

_Generated 2026-04-16 on branch `claude/sleepy-carson-G4Zbq`._

## Summary
- Oversized files: 7 (5 JS over 500 lines, 2 CSS over 800 lines)
- Feature folders with sprawl: 6 (>15 files)
- ARCHITECTURE.md drift: 7 items (1 renamed dir, 3 unlisted top-level dirs, 2 unlisted feature subfolders, 1 stale "largest feature" claim)

## Findings

### 1. Oversized Files

**JS/JSX over 500 lines**

| Lines | File | Description |
|------:|------|-------------|
| 4362 | `src/data/harvard-phoneme-meta.js` | Harvard phoneme metadata lookup table (static data) |
| 3689 | `src/data/passage-phoneme-meta.js` | Passage phoneme metadata lookup table (static data) |
|  939 | `src/data/harvard-lists.js` | Harvard sentence lists (static data) |
|  657 | `features/convo/scenarios.js` | 25 conversation scenario definitions (static data) |
|  505 | `public/vendor/d3.layout.cloud.js` | Vendor library (d3 cloud layout) |

**CSS over 800 lines**

| Lines | File | Description |
|------:|------|-------------|
|  964 | `lux-progress.css` | Progress page / dashboard styling (global) |
|  948 | `features/features/tts.css` | TTS feature styling |

**Top 3 refactor candidates per Constitution v2 (target ~200±100 lines)**

1. **`lux-progress.css` (964 lines)** — Mixes dashboard grid, chart styling, attempt-detail cards, and rollup panels in one sheet. Candidate for split into 3–4 concern-scoped files mirroring the four major progress sub-areas. No cross-file selector coupling blocks this.
2. **`features/features/tts.css` (948 lines)** — Already co-exists with `features/features/tts-peekaboo.css` (316 lines) and the `tts/` subfolder. Candidate for split along the same seams: player-shell, player-controls, karaoke row, waveform. Folder name weirdness (`features/features/`) should be addressed separately.
3. **`features/convo/scenarios.js` (657 lines)** — Flat array of 25 scenario objects. Candidate for shard-per-scenario under `features/convo/scenarios/` with a barrel `index.js`. Touches no behavior — safe seam.

### 2. Module Sprawl

**Feature folders (>15 files = flagged)**

| Files | Folder | Notes |
|------:|--------|-------|
| 61 | `features/progress/` | Far over the Constitution's ≤6-modules guideline. Heaviest folder in the repo. Genuine refactor candidate — likely split into `progress/dashboard/`, `progress/wordcloud/`, `progress/rollups/`, `progress/render/` (wordcloud already has its own 15-file subtree). |
| 38 | `features/convo/` | Already acknowledged as "the largest feature" in ARCHITECTURE.md (doc claims 36). Sprawl is structurally driven by the scenario picker + drawers + knobs + turn cycle. Known pattern; pick-deck already nested. |
| 36 | `features/interactions/` | Phoneme hover tooltips + metric modal + score-tile logic. Worth inspecting whether metric-modal render-parts and ph-hover rendering can be pulled into their own subtrees. |
| 30 | `features/features/` | Unusual nested name (`features/features/`) contains `tts/` and `selfpb/`. Naming itself is drift from any normal convention — rename to `features/tts-selfpb/` or flatten into sibling features. Sprawl here is aggregate across two sub-islands. |
| 21 | `features/streaming/` | ARCHITECTURE.md calls this "the cleanest feature island." 21 files is over threshold but the pattern (state/store + transport + render loop) is explicitly endorsed as reference architecture. Stable pattern — note only. |
| 16 | `features/my-words/` | Only just over threshold. Minor. |

**Core / shared folders (should stay small and tight)**

| Files | Folder | Assessment |
|------:|--------|-----------|
| 10 | `app-core/` | ✅ Tight. Matches the "spine" charter. |
|  2 | `core/` | ✅ Tight — but not mentioned in ARCHITECTURE.md (see drift). |
|  7 | `helpers/` | ✅ Tight. |
| 22 | `ui/` | ⚠️ Growing. Over the "small and tight" bar but not a sprawl emergency. Worth a pass next week to see whether `ui-ai-ai-*.js` pair and `lux-card` component subfolder belong inside their respective feature areas. |

### 3. Doc Drift (`docs/ARCHITECTURE.md` vs disk)

**Listed in ARCHITECTURE.md, missing on disk**

- `api/` — The doc's Directory Structure block (line 105) and Backend Endpoints table reference `api/ai.js`, `api/util.js`, etc. Actual path is `_api/` (underscore prefix). All 8 listed files do exist under `_api/`; this is a rename, not a deletion, but the doc is stale.

**Present on disk, not mentioned in ARCHITECTURE.md**

Top-level:
- `core/` (2 files) — exists but not in the Directory Structure block.
- `public/` (contains `vendor/d3.layout.cloud.js` among others) — not listed.
- `tools/` — not listed.

Under `features/`:
- `features/practice-highlight/` — not listed in the features enumeration.
- `features/voice-mirror/` (2 files, includes 478-line `voice-mirror.js` and 457-line `voice-onboarding.js`) — not listed.

**Stale claims**

- Line 117 / line 298: "AI Conversations … 36 files in features/convo/ … the biggest and most complex feature." Current reality: `features/convo/` has 38 files, and `features/progress/` now has 61 files and is the largest feature surface. The doc's "biggest" claim should be updated or qualified (progress is larger by file count; convo may still be most behaviorally complex).

## Notes (stable patterns noted but not flagged)

- **`src/data/harvard-phoneme-meta.js` (4362 lines) and `passage-phoneme-meta.js` (3689 lines) and `harvard-lists.js` (939 lines)** are reference data tables, not behavioral code. Splitting data modules purely for line count adds indirection without reducing cognitive load. These are a stable pattern — leave alone.
- **`public/vendor/d3.layout.cloud.js` (505 lines)** is vendor code. Out of scope for refactor.
- **`app-core/lux-bus.js`** was called out in the pre-flight guidance as a known-large central hub. It did not exceed the 500-line threshold this week (not present in the over-500 list), so no entry was generated — noted here purely as confirmation that the central hub remains within bounds.
- **`features/streaming/` at 21 files** is explicitly endorsed by ARCHITECTURE.md as the reference-architecture island. File count is over threshold but the pattern is intentional. Treat as stable.
- Underscore folders `_ARCHIVE/`, `_agents-archive/`, `_parts/` were excluded from scan per scope rules. `_api/` was included (per the "underscore folders except _api/" carve-out) and is where the "api/ is missing" drift finding originates.

---

_Report only — no fixes applied. Report size well under the 2000-word cap._
