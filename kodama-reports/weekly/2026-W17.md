# Weekly Architecture Audit — 2026 Week 17

_Run date: 2026-04-23 · Sequential execution (no parallel sub-agents)._

## Summary
- Oversized files: 7 total (2 actionable refactor candidates after stable-pattern filter)
- Feature folders with sprawl: 6 (`progress`, `convo`, `interactions`, `features`, `streaming`, `my-words`); `ui/` also over threshold
- ARCHITECTURE.md drift: 1 top-level directory present on disk but not documented (`_ARCHIVE`)

## Findings

### 1. Oversized files

| File | Lines | Description |
|---|---:|---|
| `src/data/harvard-phoneme-meta.js` | 4362 | AUTO-GENERATED phoneme index (`scripts/build-harvard-phonemes.mjs`) |
| `src/data/passage-phoneme-meta.js` | 3689 | AUTO-GENERATED passage phoneme index |
| `src/data/harvard-lists.js` | 939 | Pure data — 72 Harvard lists × 10 sentences |
| `features/convo/scenarios.js` | 657 | Source of truth for 25 AI conversation scenarios |
| `public/vendor/d3.layout.cloud.js` | 505 | Third-party vendor bundle |
| `lux-progress.css` | 964 | Progress UI stylesheet (user-first progress shell) |
| `features/features/tts.css` | 948 | TTS drawer + Self Playback stylesheet |

**Stable-pattern filter:** the two `*-phoneme-meta.js` files are build-script output and the d3 file is vendored; none are refactor candidates. `harvard-lists.js` and `scenarios.js` are declarative data; splitting them buys little.

**Top 3 refactor candidates (per Refactor Constitution v2, target ~200±100 lines, ≤6 modules per area):**

1. **`lux-progress.css` (964 lines)** — single monolithic stylesheet at repo root. Analogous CSS split already exists elsewhere (`lux-results.*.css`, `lux-layout.*.css`, `lux-widgets.*.css`). Natural fracture lines: shell/scaffold, dashboard tiles, rollups, attempt detail, wordcloud frame. Candidate split: `lux-progress.shell.css`, `lux-progress.tiles.css`, `lux-progress.rollups.css`, `lux-progress.detail.css`.
2. **`features/features/tts.css` (948 lines)** — lives at `features/features/` top level rather than beside its component folder (`features/features/tts/`). Doubly odd because `features/features/selfpb-peekaboo.css`, `features/features/self-playback.css`, and `features/features/tts-peekaboo.css` also sit at the parent level. Consolidation or relocation into `tts/` and `selfpb/` subfolders would align with the Refactor Constitution feature-island rule.
3. **`features/convo/scenarios.js` (657 lines)** — borderline. Pure data with a well-commented overhaul history. If it grows past ~1000, split by scenario category (workplace/social/travel/etc.). Not urgent this week.

### 2. Module sprawl

Feature folders with >15 files (file count includes nested subfolders):

| Folder | Files | Note |
|---|---:|---|
| `features/progress/` | 61 | Known largest surface — contains `attempt-detail/`, `render/dashboard/`, `rollups/`, `wordcloud/render/`. Internal sub-foldering is doing real work; flag as watch-item, not cleanup. |
| `features/convo/` | 38 | Documented in ARCHITECTURE.md as "largest feature" (38 files). Matches the doc — stable. |
| `features/interactions/` | 36 | Mix of top-level JS files and two subfolders (`metric-modal/`, `ph-hover/`). Flat structure — candidate for continued sub-foldering along metric vs. hover vs. tooltip axes. |
| `features/features/` | 30 | Name is awkward (nested `features/features/`). Contents split between `tts/` and `selfpb/` subfolders plus top-level CSS and `08-selfpb-peekaboo.js`. Documented in ARCHITECTURE.md as "TTS + Self-Playback (nested by history)." Known pattern, not a regression. |
| `features/streaming/` | 21 | Already sub-foldered (`audio/`, `prompt/`, `setup/`, `state/`, `transport/`, `ui/`). ARCHITECTURE.md calls this the "reference architecture" island — stable. |
| `features/my-words/` | 16 | Flat `panel-*.js` family (data/dom/events/render/utils). Mild sprawl; on the cusp. No action. |

`features/results/` is at 15 (right at threshold, not flagged). Also contains `index.js.bak` — a dangling backup file that should be removed or moved to `_ARCHIVE/`; noted but not part of sprawl count.

Utility folders:

| Folder | Files | Note |
|---|---:|---|
| `app-core/` | 10 | Tight. ✅ |
| `core/` | 2 | Tight (with `prosody/` + `scoring/` subfolders). ✅ |
| `helpers/` | 7 | Tight. ✅ |
| `ui/` | 22 | **Over the "small and tight" bar.** Mix of cross-cutting effects (ripple, arrow-trail, warp), auth DOM, AI Coach DOM+logic, the `components/` card system, and `lux-warn.js`. Worth a judgment call: the `ui-ai-ai-*` cluster (DOM + logic + logic subfolder) is arguably its own feature and could live under `features/ai-coach/`. Flag for discussion, not immediate action. |

### 3. ARCHITECTURE.md drift

ARCHITECTURE.md dated 2026-04-19 compared against current disk layout.

**Directories mentioned in ARCHITECTURE.md that no longer exist on disk:** none. Every documented directory (`src/`, `app-core/`, `_api/`, `core/`, `prosody/`, `features/*`, `ui/`, `helpers/`, `_parts/`, `tests/`, `tools/`, `admin/`, `scripts/`, `docs/`, `_agents-archive/`, `kodama-reports/`, `public/`) is present. All 17 documented feature subfolders exist.

**Top-level directories present on disk but not mentioned in ARCHITECTURE.md:**

- **`_ARCHIVE/`** — top-level archive directory, not referenced anywhere in ARCHITECTURE.md. Exists alongside the documented `_agents-archive/`. Recommend a one-line mention so future readers aren't confused by the two archive folders.
- `.codesandbox/` and `.routine-state/` — tooling/state directories. Below the architecture bar; no action needed.

## Notes (stable patterns — noted, not flagged)

- **Auto-generated data files** (`harvard-phoneme-meta.js`, `passage-phoneme-meta.js`): large by design, regenerated by `scripts/build-*.mjs`. Do not hand-edit; do not refactor.
- **Vendor bundle** (`public/vendor/d3.layout.cloud.js`): third-party, keep as-is.
- **Data-only files** (`harvard-lists.js`, `scenarios.js`): declarative content, size is a function of curriculum not code complexity.
- **`features/progress/` at 61 files**: progress is the dashboard + wordcloud + rollups + attempt-detail surface; sub-foldering is already in place and meaningful. Large but structured.
- **`features/convo/` at 38 files**: explicitly called out in ARCHITECTURE.md as the largest feature; matches documented reality.
- **`features/features/` naming**: awkward but intentionally preserved per doc note "nested by history."
- **`lux-bus.js` (app-core)** was not flagged as oversized this week (below threshold); central-hub status noted in case future growth reaches the bar.

## Scope reminders

- Scanned `.js`, `.jsx`, `.html`, `.css` under repo root.
- Skipped: `node_modules/`, `dist/`, `.vite/`, `_ARCHIVE/`, `_agents-archive/`, `_parts/`.
- Dead-code detection, prosody dual-home check, and deep dependency graph analysis were dropped from this routine per the v2 brief (timeout-prone or phase-specific).
- Report only. No fixes made.
